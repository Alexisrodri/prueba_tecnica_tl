name: prueba_tecnica_tl
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  file_picker: ^8.1.2
  flutter:
    sdk: flutter
  go_router: ^14.2.7
  syncfusion_flutter_pdfviewer: ^26.2.12

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
